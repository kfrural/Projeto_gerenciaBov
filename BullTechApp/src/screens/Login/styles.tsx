import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    container: {
        backgroundColor: "#fff",
    },
    title: {
        color: "#000",
    },
    text: {
        color: "#000"
    },
});

export default style;